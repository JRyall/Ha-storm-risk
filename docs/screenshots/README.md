# Screenshots

The README references the following screenshots. They are not committed yet —
capture them from a running Home Assistant instance and drop them in here:

- `config_flow.png` — the **Add integration** dialog (name + map picker).
- `options_flow.png` — the **Configure** dialog (scoring divisors and
  thresholds).

A width of roughly 600–800 px renders nicely in the README.
